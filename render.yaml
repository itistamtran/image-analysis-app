services:
  - type: web
    name: flask-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    workingDir: backend
    envVars:
      - key: FLASK_ENV
        value: production